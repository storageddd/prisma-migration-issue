{
  "name": "prisma-migration-issue",
  "version": "1.0.0",
  "author": "storageddd",
  "private": true,
  "scripts": {
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:deploy": "npx prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "3.7.0"
  },
  "devDependencies": {
    "prisma": "3.7.0"
  }
}
